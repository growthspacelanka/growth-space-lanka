'use client';

import React from 'react';
import PageHeader from '@/components/common/PageHeader';

const AiHero = () => {
    return (
        <PageHeader
            titleKey="services.ai.page.title"
            descriptionKey="services.ai.page.description"
        />
    );
};

export default AiHero;